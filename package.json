{
  "name": "cypress-demo",
  "version": "1.0.0",
  "description": "\"Demo Code to run cypress test\"",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --env allure=true",
    "chrome": "npx cypress run --env allure=true --browser chrome",
    "firefox": "npx cypress run --env allure=true --browser firefox",
    "edge": "npx cypress run --env allure=true --browser edge",
    "report": "npx allure generate allure-t --clean -o allure-report"
  },
  "author": "Veerkumar Patil",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.3.1",
    "@shelex/cypress-allure-plugin": "^2.26.5",
    "allure-commandline": "^2.17.2"
  }
}
